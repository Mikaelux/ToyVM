- Currently loops are causing an issue, mainly because i dont have a ntive
incrementation method probably 
	+ add instr_add capability to add directly into register value
		(by subsequence, every other operation)
